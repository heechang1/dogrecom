# Dependencies
node_modules

# Build
dist
dist-ssr
.next
out
*.local

# Environment (비밀키·로컬 설정 — 커밋 금지)
# - .env.local, .env.development.local 등은 .env.* 에 포함되어 무시됨
# - 기타 *.local 파일도 상단 *.local 규칙으로 무시됨
.env
.env.*
!.env.example

# Logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
